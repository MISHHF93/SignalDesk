// The `@modal` parallel slot's fallback for every route under `/integrations`
// that isn't the intercepted `(.)[slug]` drawer — i.e. no drawer is open.
export default function Default() {
  return null;
}
